import { Mapper, MapperOptions } from './mapper';
export declare class Mapper075 extends Mapper {
    private options;
    static create(options: MapperOptions): Mapper;
    constructor(options: MapperOptions);
}
